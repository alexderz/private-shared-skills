# LLD — <title>

- Slug: `<chunk-slug>`
- HLD: `docs/<path>`
- Tickets this LLD covers: `<ids>`
- Date: `YYYY-MM-DD`

## Required

- **Paths / modules** — where code and configs live
- **Behavior** — what each part does; enough to groom Tasks
- **Trust boundaries** — authn/z, secrets, egress, data class, who may
  write what. If none: `n/a` and why (security still reads this)
- **Mockups** — `docs/<path>` if people see a screen, else `n/a` + why.
  Operator accepted or waived. Spec step, not Plan.
- **Verify** — proving commands / checks for DoD
- **Land** — project-main name; ticket IDs on PR/changelog

## Optional

- Status mapping (board states ↔ after-act)
- Interfaces / schemas
- Rollout / rollback
- Bot after-act notes
