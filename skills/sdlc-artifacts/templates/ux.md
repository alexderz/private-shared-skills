# UX — <title>

High-level. How it feels and who does what — not pixels. **designer.**
Human gate: operator accepts, or writes `UX verification not required`.

- Slug: `<chunk-slug>`
- Brief: `<link>`
- HLD: `docs/<path>`
- Date: `YYYY-MM-DD`
- Agent review (requirements only): agreed `YYYY-MM-DD` / pending
- Operator: accepted `YYYY-MM-DD` / waived / pending

## Required

- **People** — who uses this
- **Journeys** — the few paths that matter (happy + the important failure)
- **Stories** — list of `user-story` ids
- **High-level UX** — what they see and do, in words or a rough sketch
- **Comparables** — same `docs/<comparables>` as HLD, plus what we steal
  or reject for **look and flow**
- **Not this** — screens or flows we are not doing

## Optional

- Accessibility notes at this grain
- Open questions for mockups
