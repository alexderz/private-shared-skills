# Mockups — <title>

LLD grain. Pictures or linked frames for screens people will see.
**designer.** If there is no screen: `n/a` and why. Human gate: operator
accepts, or writes `UX verification not required`.

- Slug: `<chunk-slug>`
- UX / stories: `docs/<path>`
- LLD: `docs/<path>`
- Date: `YYYY-MM-DD`
- Operator: accepted `YYYY-MM-DD` / waived / pending

## Required

- **Medium** — markdown / HTML / canvas / image / attached tool + git
  snapshot
- **Variants** — 2–3 structurally different options (A/B/C), or `n/a` +
  why one is enough
- **Surfaces** — each screen or main state
- **Files or links** — in git. No fake “see chat.”
- **Map to stories** — which picture is which story
- **Empty / error / loading** — at least named
- **Agent review** — requirements-only; agreed `YYYY-MM-DD` / pending

## Optional

- Notes for the builder (spacing is not a substitute for the picture)
